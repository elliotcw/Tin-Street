define(function (require) {
    'use strict';

    var angular = require('angular');

    angular.module('portfolio', []);//, ['portfolio.controllers', 'portfolio.services', 'portfolio.services']);
});